services:
  webui:
    image: ghcr.io/open-webui/open-webui:main
    ports: ["3000:8080"]
    environment:
      - WEBUI_AUTH=False
      - HOLIDAY_TOOL_BASE_URL=http://nlp-api:8000
      - HOLIDAY_TOOL_MODE=direct-parse
      - HOLIDAY_TOOL_METHOD=rules
    volumes:
      - webui_data:/app/backend/data

  nlp-api:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - INTERACTION_MODE=direct-parse
      - FIXTURES_DIR=/app/fixtures
      - CSV_PATH=/logs/transactions.csv
    volumes:
      - ./fixtures:/app/fixtures:ro
      - ./logs:/logs

volumes:
  webui_data: